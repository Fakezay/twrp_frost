name: Compilar TWRP - Poco C40 (frost)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout do seu repo
      uses: actions/checkout@v4

    - name: 🗑️ Limpeza Extrema de Disco (Liberar +30GB)
      run: |
        # Apaga softwares pesados que não vamos usar (Android SDK, .NET, CodeQL)
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/graalvm
        sudo rm -rf /usr/local/.ghcup
        sudo rm -rf /usr/local/share/powershell
        sudo rm -rf /usr/local/lib/node_modules
        sudo rm -rf /opt/ghc
        sudo rm -rf /usr/local/share/boost
        # Limpa o cache do apt
        sudo apt-get clean
        # Mostra o espaço livre final
        df -h

    - name: 🐳 Compilar dentro do Docker (Ubuntu 20.04)
      # Aqui está o segredo: Rodamos o Docker manualmente mapeando o disco limpo
      run: |
        docker run --rm -v ${{ github.workspace }}:/workspace -w /workspace ubuntu:20.04 /bin/bash -c "
          # 1. Configurar o ambiente dentro do container
          apt-get update && \
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            git-core gnupg flex bison build-essential zip curl zlib1g-dev \
            gcc-multilib g++-multilib libc6-dev-i386 libncurses5 lib32ncurses5-dev \
            x11proto-core-dev libx11-dev lib32z1-dev libgl1-mesa-dev libxml2-utils \
            xsltproc unzip fontconfig python2 python3 rsync sudo && \
          
          # Linkar python
          ln -s /usr/bin/python2 /usr/bin/python && \

          # 2. Instalar o Repo
          curl https://storage.googleapis.com/git-repo-downloads/repo > /usr/bin/repo && \
          chmod a+x /usr/bin/repo && \

          # 3. Configurar Git
          git config --global user.name 'Fakezay' && \
          git config --global user.email 'fakezaydev@gmail.com' && \

          # 4. Sincronizar Código (Com otimizações de espaço)
          mkdir twrp && cd twrp && \
          repo init --depth=1 -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git -b twrp-11 && \
          repo sync -c --no-clone-bundle --no-tags --optimized-fetch --prune --force-sync -j$(nproc --all) && \

          # 5. Clonar sua Device Tree
          git clone https://github.com/Fakezay/twrp_frost device/xiaomi/frost && \

          # 6. Compilar
          export ALLOW_MISSING_DEPENDENCIES=true && \
          . build/envsetup.sh && \
          lunch omni_frost-eng && \
          mka recoveryimage -j$(nproc --all) && \
          
          # Mover o arquivo final para fora do container
          cp out/target/product/frost/recovery.img /workspace/recovery.img
        "

    - name: 📤 Upload do Resultado
      uses: actions/upload-artifact@v4
      with:
        name: TWRP-frost-PocoC40
        path: recovery.img
